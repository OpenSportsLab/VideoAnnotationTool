[pytest]
markers =
    gui: marks tests that exercise PyQt GUI lifecycle flows.
    network: marks opt-in tests that access external services.
    slow: marks tests that download or initialize large models.
